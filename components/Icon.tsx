import React from 'react';
import { Lock, Unlock, Check, Send, Shield, Activity, Music, RefreshCw } from 'lucide-react';

// Wrapper to ensure consistent sizing/styling if needed
export const Icons = {
  Lock,
  Unlock,
  Check,
  Send,
  Shield,
  Activity,
  Music,
  Reset: RefreshCw
};
